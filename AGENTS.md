# AGENTS.md

## 1. 프로젝트 정보
- **프로젝트 타입**: Unreal Engine C++ 기반 프로젝트
- **빌드 시스템**: UnrealBuildTool (UBT)

## 2. 폴더 제외 규칙 (토큰 절약)
다음 폴더 내부의 모든 파일은 읽기, 탐색, 인덱싱 대상에서 제외합니다:
- `Binaries/`
- `Intermediate/`
- `Saved/`
- `DerivedDataCache/`
- `.git/`

## 3. 메모리 및 언리얼 코딩 컨벤션
- **Epic Games 공식 언리얼 C++ 스타일 준수**
- **UObject 포인터 관리**: 가비지 컬렉션 누수 방지를 위해 반드시 `UPROPERTY()` 매크로로 관리할 것.
- **비-UObject 메모리 관리**: `TSharedPtr`, `TWeakPtr`, `TWeakObjectPtr` 등 스마트 포인터 활용.
- **언리얼 전용 타입 사용**: `FString`, `FName`, `FText`, `int32`, `TArray` 등 언리얼 전용 타입 사용.

## 4. 응답 및 소통 스타일
- **한국어 작성**: 사용자와의 모든 소통, 코드 설명, 코드 내 주석은 한국어로 작성할 것.
- **간결한 출력**: 서론/인사말은 생략하고 핵심 코드 변경점과 수정 또는 생성이유을 로직 순서대로 설명할 것. 또한 변경된 모든 파일명을 명시할 것.
- **코드 수정하기 전**: 왜 수정하려고 하는지 뭘 수정하려는지 먼저 보고할 것.

## 5. `[loop]` Command (Self-Refinement)
사용자의 프롬프트에 `[loop]` 키워드가 포함되어 있는 경우, 다음과 같은 **3단계 자가 피드백(Self-Feedback) 절차**를 거쳐 최종 답변을 생성한다. 그리고 3단계 자가 피드백을 거친 답변이면 답변 끝에 "[loop]"라고 남긴다. 반복횟수를 표시한다.

1. **[1차 답변 생성]**: 사용자의 질문에 대한 기본 답변을 내부적으로 구성한다.
2. **[자가 검토/비판]**: 기본 답변에서 발생할 수 있는 아래 요소를 스스로 검토한다.
   - 논리적 오류, 예외 처리 누락, 엣지 케이스
   - 가독성 및 코드/설명의 명확성
   - 개선할 수 있는 성능 및 최적화 요소
3. **[최종 출력]**: 검토 결과를 반영하여 완성도를 극대화한 최종 답변만을 사용자에게 출력한다. (검토 과정은 필요시 짧게 요약하거나 생략하고 최적의 결과에 집중한다.)

## 6. 파일생성
- PR은 사용자가 요청할때 생성할 것.
- md파일, HTML파일을 생성할때 "C:\Users\user\Documents\AI_MD" 여기에 생성할 것.
- PR을 생성할때 md파일로 생성할 것
-  PR을 작성할때 수정 작업이면 -> "이전 코드가 어떤 구조였는데 왜 코드를 바꿨고, 어떻게 바꿨고, 바꿔서 기대할 수 있는게 이런게 있다" 구조로 작성할 것
- PR을 작성할때 생성 작업이면 -> "왜 이런 기능이 필요했고, 프로젝트 안에서 어느 논리 구조로 작동하며, 어떤 방법을 사용했다" 구조로 작성할 것
- 작업이 수정, 생성 둘 다 있으면 PR작성에 두 요구사항 모두 적용할 것